import { createUseStyles } from 'react-jss'

export const useStyles = createUseStyles({
  aside:{
    position: 'absolute',
    paddingTop: '20px',
    paddingLeft: '1rem',
    maxWidth: '10rem',
    height: '100%',
    fontSize: '0.7rem',
  }
});